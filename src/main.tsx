import React from 'react'
import ReactDOM from 'react-dom/client'
import DemoOne from './demo'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><DemoOne /></React.StrictMode>)
