/**
 * Optional code-based OmniRoute configuration.
 *
 * For local/private use you may paste values below. IMPORTANT: anything in this
 * file is bundled into the browser and can be viewed by visitors. For a public
 * deployment, use the hidden admin panel and preferably a server-side proxy.
 */
export const OMNIROUTE_CONFIG = {
  baseUrl: 'https://openrouter.ai/api/v1',
  apiKey: '', // Example: 'your-omniroute-api-key'
  defaultModel: '', // Leave blank to automatically use the first available model.
}
